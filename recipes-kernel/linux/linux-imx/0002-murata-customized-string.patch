Do not treat warnings as errors

There are additional warnings found with musl which are
treated as errors and fails the build, we have more combinations
than upstream supports to handle.

Upstream-Status: Inappropriate [oe specific]
From 7b6ee598d81ddfac24f9ff324aedc2eebe68104e Mon Sep 17 00:00:00 2001
From: Jameel Kareem <jkareem@murata.com>
Date: Tue, 26 May 2026 14:05:01 -0700
Subject: [PATCH 2/3] murata-customized-string

---
 drivers/net/wireless/broadcom/brcm80211/brcmfmac/common.c | 1 +
 1 file changed, 1 insertion(+)

diff --git a/drivers/net/wireless/broadcom/brcm80211/brcmfmac/common.c b/drivers/net/wireless/broadcom/brcm80211/brcmfmac/common.c
index bf7395cf10e2..ee57df7fbf34 100644
--- a/drivers/net/wireless/broadcom/brcm80211/brcmfmac/common.c
+++ b/drivers/net/wireless/broadcom/brcm80211/brcmfmac/common.c
@@ -571,6 +571,7 @@ int brcmf_c_preinit_dcmds(struct brcmf_if *ifp)
 	strsep(&ptr, "\n");
 
 	/* Print fw version info */
+    brcmf_info("Murata Customized Version: imx-walnascar-longma_r1.0\n");
 	brcmf_info("Firmware: %s %s\n", ri->chipname, buf);
 
 	/* locate firmware version number for ethtool */
-- 
2.25.1

